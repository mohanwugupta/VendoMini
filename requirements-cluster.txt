# VendoMini Cluster Requirements
# Install with: pip install -r requirements-cluster.txt

# Core dependencies
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
pytest>=7.4.0
pytest-cov>=4.1.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0

# HuggingFace/PyTorch for LLM inference
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
protobuf>=3.20.0
sentencepiece>=0.1.99

# Optional but recommended for better performance
safetensors>=0.4.0
einops>=0.7.0

# Optional: Flash Attention 2 for faster inference (requires Ampere+ GPUs)
# Install with: pip install flash-attn --no-build-isolation
flash-attn>=2.3.0

# Optional: vLLM for optimized multi-GPU inference (much faster than standard transformers)
# Enable with: export VENDOMINI_USE_VLLM=1
# Install with: pip install vllm>=0.2.0
vllm>=0.2.0
